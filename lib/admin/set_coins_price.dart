import 'package:flutter/material.dart';

class CoinPrice extends StatelessWidget {
  const CoinPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
